'use client';

import { LayoutProps, } from '@modules/layout/libraries/layout-types';

const PrintLayout = ({ children, }: LayoutProps) => {

  return (
    <>
      {children}
    </>
  );
};

export default PrintLayout;
